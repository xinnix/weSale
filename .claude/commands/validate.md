全栈验证 — 依次运行类型检查、lint、构建和测试，输出汇总报告。

执行以下验证步骤并汇总结果：

1. **类型检查**: `pnpm -r run type-check`（如果配置了）
2. **Lint**: `pnpm -r run lint`（如果配置了）
3. **构建**: `pnpm -r run build`
4. **测试**: `pnpm -r run test`（如果配置了）

对每一步记录通过/失败状态，最后输出汇总表：

```
| 步骤       | 状态 | 耗时   |
|-----------|------|--------|
| type-check| ✅   | 2.3s   |
| lint      | ⚠️   | 1.1s   |
| build     | ✅   | 12.5s  |
| test      | ❌   | 5.2s   |
```

如果任何步骤失败，在汇总后列出具体的错误信息。
